import React from 'react';
import { Layout } from 'antd';
import './header.less';

const { Header } = Layout;

const AppHeader = () => {
  return (
    <Header className="header">
    </Header>
  );
};

export default AppHeader;
